cd ./api

npm install
if [ $? -ne 0 ]; then
    echo "npm install failed in the 'api' directory"
    exit 1
fi

cd ../frontend

npm install
if [ $? -ne 0 ]; then
    echo "npm install failed in the 'frontend' directory"
    exit 1
fi

cd ..
echo "npm install completed successfully in both directories"