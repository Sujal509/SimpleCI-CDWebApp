cd .\api

npm install
if ($LASTEXITCODE -ne 0) {
    Write-Output "npm install failed in the 'api' directory"
    exit $LASTEXITCODE
}

cd ../frontend

npm install
if ($LASTEXITCODE -ne 0) {
    Write-Output "npm install failed in the 'frontend' directory"
    exit $LASTEXITCODE
}

Write-Output "npm install completed successfully in both directories"
